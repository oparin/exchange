{% extends 'base.html.twig' %}

{% block styles %}
    {{ parent() }}
    <link rel="stylesheet" href="{{ asset('lib/metis/assets/css/dataTables.bootstrap.css') }}">
{% endblock styles %}

{% block title_content %}<i class="fa fa-file-o"></i>&nbsp; Статические страницы{% endblock title_content %}

{% block content %}
    <div class="row">
        <div class="col-lg-12">
            <div class="box">
                <header>
                    <div class="icons">
                        <i class="fa fa-table"></i>
                    </div>
                    <h5>Все страницы</h5>
                </header>
                <div id="collapse4" class="body">
                    <table class="table table-bordered table-condensed table-hover table-striped dataTable">
                        <thead>
                        <tr class="grid-row-titles">
                            <th class="sorting " style="color: black; width: 40%">Страница</th>
                            <th class="sorting " style="color: black; width: 40%">Локаль</th>
                            <th class="last-column" style="width: 10%"></th>
                        </tr>
                        </thead>
                        <tbody>
                        {% for page in pages %}
                            <tr class="grid-row-cells even">
                                <td class="grid-column-lastIp">{{ page.title }}</td>
                                <td class="grid-column-lastIp">{{ page.locale }}</td>
                                <td class="" style="text-align: center">
                                    <a target="_self" href="{{ path('admin_static_page', {'id': page.id}) }}" class="action-icon" style="margin-left: 10px">
                                        <i class="glyphicon glyphicon-edit"></i>
                                    </a>
                                    {#<a target="_self" href="{{ path('delete_faq', {'id': faq.id}) }}" class="action-icon" style="margin-left: 10px">#}
                                        {#<i class="glyphicon glyphicon-remove"></i>#}
                                    {#</a>#}
                                </td>
                            </tr>
                        {% endfor %}
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
{% endblock content %}